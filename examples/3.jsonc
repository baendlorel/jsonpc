{
  "aaa": [
    // ddd
    { "a": 1 },
    // sdf
    [23],
  ],

  "aada":
    // ! 不允许在冒号下面一行出现注释
    "asdf",
  // ! 不允许注释的下一行是逗号或者是?}
}

// EPIC 添加一种允许的情况就是：在对象数组的每一个对象的上方，允许写注释
// 这种注释的位置是确定的，是：
// 1、在[和{之间，那就在{后面加一个属性
// 2、在,和{之间，那就在{后面加一个属性
// 3、在[和[之间，那就在[后面加一个元素
// 4、在,和[之间，那就在[后面加一个元素
// 5、注释的前面是逗号或者[，注释的后面是字符串、数字、null、boolean且不是keyvalue对。
